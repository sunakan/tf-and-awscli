ARG ALPINE_PYTHON_VERSION_TAG
FROM python:${ALPINE_PYTHON_VERSION_TAG}
ENV PYTHONIOENCODING=UTF-8
RUN apk add --no-cache jq
ARG AWS_CLI_VERSION
RUN pip install --no-cache-dir awscli==${AWS_CLI_VERSION}
ENTRYPOINT ["aws"]
